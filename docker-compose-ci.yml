version: "3.0"

services:
  db:
    image: wandersuite/wander:${TAG_VERSION}
    environment:
      - HOST=${HOST}
      - POSTGRES_DB=${DBNAME}
      - POSTGRES_PASSWORD=${DBPASS}
      - POSTGRES_USER=${DBUSER}
    ports:
      - 5432:5432
